rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DEVELOPMENT RULES: All permissions granted.
      allow read, write: if true;
    }
  }
}
